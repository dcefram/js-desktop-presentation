import React from 'react';
import { render } from 'react-dom';
import Hello from './Hello';

const styles = {
  fontFamily: 'sans-serif',
  textAlign: 'center',
};

const App = () => (
  <div style={styles}>
    <Hello name="Sample Component" />
    <h2>Just an example :D</h2>
  </div>
);

render(<App />, document.getElementById('root'));
